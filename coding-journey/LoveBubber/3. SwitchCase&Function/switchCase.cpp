// get notes value   H/W
/*
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;

    int num = 2000;
    switch (num)
    {
    case 2000:
        cout << "2000 note " << n / 2000 << endl;
        n = n - (n / 2000) * 2000;
    case 500:
        cout << "500 note " << n / 500 << endl;
        n = n - (n / 500) * 500;
    case 200:
        cout << "200 note " << n / 200 << endl;
        n = n - (n / 200) * 200;
    case 100:
        cout << "100 note " << n / 100 << endl;
        n = n - (n / 100) * 100;
    case 50:
        cout << "50 note " << n / 50 << endl;
        n = n - (n / 50) * 50;
    case 20:
        cout << "20 note " << n / 20 << endl;
        n = n - (n / 20) * 20;
    case 10:
        cout << "10 note " << n / 10 << endl;
        n = n - (n / 10) * 10;
    case 5:
        cout << "5 note " << n / 5 << endl;
        n = n - (n / 5) * 5;
    case 2:
        cout << "2 note " << n / 2 << endl;
        n = n - (n / 2) * 2;
    case 1:
        cout << "1 note " << n / 1 << endl;
        n = n - (n / 1) * 1;
    }

    return 1;

*/

/*
    1. AP --> n given ap return using function
    2. a&b total no of set bits
    3. return n-th fibonacci number
*/